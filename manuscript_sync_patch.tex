% Small manuscript corrections identified by executable verification.
% These are corrections to the revised excerpt supplied on 30 August 2026.

% 1. In the paragraph defining the stochastic two-dimensional initial data,
% add the seed explicitly:
where the entries of $Z^u$ and $Z^v$ are independent $N(0,1)$ variates
generated by NumPy's \texttt{default\_rng} with seed $1$.

% 2. In Table tab:grid_2d, the stationarity values in the first two rows should
% follow ordinary rounding of the executable outputs.  Replace 1.42 and 2.19
% by 1.41 and 2.18, respectively:
% 40  ... & $1.41\times10^{-12}$ & 1.024\\
% 80  ... & $2.18\times10^{-12}$ & 1.024\\
% 160 ... & $2.50\times10^{-12}$ & 1.024\\
% 320 ... & $2.78\times10^{-12}$ & 1.024\\

% 3. In the Fig. 14 caption, replace ``bisection on the full model'' by the
% method actually used in Example 1 and threshold_analysis.py:
with the onset measured by bisection on the principal eigenvalue of the
finite-volume linearisation about the tumour-free state

% 4. The range 0.015--0.071 is not computed from identical fixed-supply
% replicates.  Replace the corresponding sentence by:
After sorting the 122 points by $\overline{\sigma}_{0,h}$ and dividing them
into six equal-count supply strata, the within-stratum residual standard
deviation ranges from $0.015$ to $0.071$.

